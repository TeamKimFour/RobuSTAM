# RobuSTAM 로컬 개발용 Docker Compose — fastapi
# 실행: docker compose up --build

services:
  fastapi:
    build:
      context: .
      dockerfile: docker/Dockerfile.api
    ports:
      - "8000:8000"
    volumes:
      - ./config:/app/config:ro
