# RobuSTAM 의존성 (Python 3.11 기준 — CI와 로컬 동일)
# 데이터 엔지니어링 레이어용. RL/서빙 의존성은 추후 별도 추가.

# 데이터 처리
pandas
numpy
pyarrow          # Parquet 입출력

# 데이터 수집
yfinance

# 설정
pyyaml

# 테스트
pytest

# NOTE: 기술적 지표용 pandas-ta는 현재 PyPI 버전(0.4.x)이 Python>=3.12를 요구해
#       3.11에서 설치 불가. 2주차 features.py 착수 시 결정 필요:
#       (a) Python 3.12로 상향, (b) 구버전 pandas-ta 고정, (c) ta 라이브러리/순수 pandas로 지표 계산.
